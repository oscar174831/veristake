{
  "name": "veristake-contracts",
  "version": "1.0.0",
  "description": "On-chain core contracts for the Veristake verifier protocol layer.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "test:heavy": "forge test --fuzz-runs 10000",
    "coverage": "forge coverage --summary --skip script --exclude-tests --no-match-coverage \"lib/.*|test/.*\""
  },
  "keywords": [],
  "author": "",
  "license": "BUSL-1.1",
  "type": "commonjs",
  "devDependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "forge-std": "^1.1.2"
  }
}
